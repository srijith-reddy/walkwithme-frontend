
import Foundation

enum DebugSettings {
    /// Show raw YOLO bounding boxes (red) — debug only
    static var showYOLOBoxes = false

    /// Show post-fusion hazard boxes (green) — debug only
    static var showFusionBoxes = false

    /// Enable 3D AR hazard icons (pedestrians, vehicles) — product feature
    static var showARHazards = true
}

